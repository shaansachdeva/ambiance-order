# Placeholder values — DO NOT USE IN PRODUCTION.
# Override locally with .env.local (gitignored) or on EC2 with .env.ec2.
# Generate a real secret with: node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
DATABASE_URL="file:./dev.db"
NEXTAUTH_SECRET="replace-me-override-in-env-local"
NEXTAUTH_URL="http://localhost:3000"
